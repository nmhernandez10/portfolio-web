dist
.astro
pnpm-lock.yaml

# design source of truth must stay byte-verbatim until its phase-7 deletion
.claude

# tokens are copied verbatim from the design system; formatting them would break
# the phase-2 `diff -r src/styles/tokens` parity check
src/styles/tokens

# the same, for the new system's tokens; VC3 renames this to src/styles/tokens
src/styles/redesign/tokens

# e2e artifacts
test-results
playwright-report
blob-report
playwright/.cache
