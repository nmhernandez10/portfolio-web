dist
.astro
package-lock.json

# local agent configuration, not repo source
.claude

# The design vocabulary, kept as the ported artifact it is. The parity diff that
# first justified this is gone with the skill, but the rule it protected is not:
# tokens/ is not edited, and letting a formatter rewrite six files nothing may
# touch would be churn against the one place the system's values live verbatim.
src/styles/tokens

# e2e artifacts
test-results
playwright-report
blob-report
playwright/.cache
