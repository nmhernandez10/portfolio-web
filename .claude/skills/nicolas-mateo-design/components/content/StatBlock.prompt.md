StatBlock — real numbers only, taken from shipped work.

```jsx
<StatBlock value="200k+" label="Sessions delivered" note="Keel Mind platform" />
```
Never invent metrics; if a number can't be sourced, drop the block rather than round something up.
