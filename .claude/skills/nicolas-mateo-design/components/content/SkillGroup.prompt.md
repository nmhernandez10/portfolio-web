SkillGroup — one category of the skills table.

```jsx
<SkillGroup title="Data" items={['PostgreSQL', 'SQL', 'Migrations', 'Redis']} />
```
Prefer slash-separated runs over tag clouds here; tags are for project cards.
