Input — contact-form field.

```jsx
<Input label="Email" type="email" placeholder="you@company.com" />
```
Labels are always mono uppercase; placeholders are lower-case examples, never a repeat of the label.
