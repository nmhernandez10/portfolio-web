Tag — a stack technology, a role, or a status marker.

```jsx
<Tag>PostgreSQL</Tag>
<Tag tone="moss" dot>Open to work</Tag>
```

Keep tag rows to one line where possible; sort by relevance, not alphabetically. Non-mono (`mono={false}`) is for human-language labels like "Remote".
