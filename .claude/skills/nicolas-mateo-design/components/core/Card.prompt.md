Card — a hairline-bordered container for a project, a note, or a grouped block.

```jsx
<Card interactive href="#project">…</Card>
```

Use `tone="sunk"` for code samples and quiet metadata blocks, `tone="inverse"` sparingly (at most one dark block per page).
