# Site B — portrait hero

Open `index.html`. One page, no routing.

- **Hero:** 7/5 split — type left, the real portrait right as a 4:5 crop with a `Bogotá · Remote` mono tab notched into its lower-left corner.
- **Work is screenshot-free:** `ProjectBrief` cards in a two-up grid. Numbered role line, outcome sentence, metrics above a hairline, stack tags. Nothing pretends a screenshot is coming.
- The portrait also appears at 36px in the header and footer, so the page is recognisably his all the way down.
- Contact is a working (fake) form: name, email, subject select, message, confirmation tag.

Content comes from `../portfolio-site/data.js`; the lens toggle in the hero retargets the page.
