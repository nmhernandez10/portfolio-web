One-line: the system's container — 14px radius, hairline border, white on warm paper, shadow only while hovered.

```jsx
<Card padding="lg">…</Card>
<Card as="a" href="/work/keel-mind" interactive>…</Card>
<Card tone="sunken" padding="md">…</Card>
```

- Static cards have no shadow. That is deliberate: structure comes from the hairline.
- `tone="accent"` is reserved for a single callout per page.
