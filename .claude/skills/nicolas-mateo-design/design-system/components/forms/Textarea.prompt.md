One-line: the message field; same skin as Input, vertical resize only.

```jsx
<Textarea label="What are you building?" rows={5} placeholder="A sentence or two is plenty." />
```

- Placeholders are written as friendly instructions, not field names.
