One-line: 42x24 pill toggle; gold track and ink knob when on.

```jsx
<Switch checked={reduced} onChange={setReduced} label="Reduce motion" />
```

- For light/dark use ThemeToggle instead — it carries the sun/moon glyph and the data-theme wiring.
