One-line: native select in the field skin, with a Lucide chevron-down instead of the OS arrow.

```jsx
<Select label="What is this about?" options={["A role", "Contract work", "Something else"]} />
```

- Keep option lists short and human ("A role", not "EMPLOYMENT_INQUIRY").
